(function (root) {
	require(["config"], function(config){
		requirejs.config(config);
		require(["ember","ember_data"], function(Ember,DS){
			
		});
	});
	//this es el windows que pasa como parametro al root
})(this);